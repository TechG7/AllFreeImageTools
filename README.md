# Pixora Rocket — Modular Website
- index.html = main file/dashboard
- tools/ = one separate HTML file per tool
- assets/style.css = shared premium UI
- assets/theme.js = dark/light mode
- assets/common.js = shared browser image helpers
Each tool can be edited independently without changing the main page.
